# To run on minikube, enter the following commands:

$ python3 -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
$ cd kubernetes
$ python render_template.py     # passing in any arguments you would like

